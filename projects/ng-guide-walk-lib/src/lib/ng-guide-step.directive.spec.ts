import { NgGuideStepDirective } from './ng-guide-step.directive';

describe('NgGuideStepDirective', () => {
  it('should create an instance', () => {
    const directive = new NgGuideStepDirective();
    expect(directive).toBeTruthy();
  });
});
